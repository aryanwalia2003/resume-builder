# This directory holds Google OAuth credentials — never commit these files.
# 
# Files in here:
#   credentials.json  — Download from Google Cloud Console (OAuth2 Desktop App)
#   token.json        — Auto-generated after running: python scripts/drive_setup.py
#
# Both are listed in .gitignore.
